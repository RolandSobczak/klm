"""Command-line interface.

A thin adapter over ``klm.services``. No business rule may live here: if the
GUI can do something the CLI cannot, that is a bug in the CLI
(docs/12-cli-and-desktop-app.md §1).
"""
